{
  "name": "@amamo/verso",
  "version": "1.0.0-rc.4",
  "description": "A focused release CLI for JavaScript workspace packages.",
  "license": "MIT",
  "author": "白熱 <sonne@asaki.me>",
  "homepage": "https://github.com/jikkai/verso#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jikkai/verso.git",
    "directory": "packages/verso"
  },
  "bugs": {
    "url": "https://github.com/jikkai/verso/issues"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "keywords": [
    "release",
    "changelog",
    "semver",
    "workspace",
    "monorepo",
    "cli"
  ],
  "type": "module",
  "bin": {
    "verso": "bin/verso.js"
  },
  "files": [
    "bin",
    "dist",
    "package.json",
    "README.md"
  ],
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/rewrite-dist-imports.mts && node scripts/build-bin.mts",
    "prepack": "pnpm run build",
    "prepublishOnly": "pnpm run build",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.scripts.json --noEmit",
    "test": "pnpm run build"
  },
  "optionalDependencies": {
    "@amamo/verso-darwin-arm64": "workspace:*",
    "@amamo/verso-darwin-x64": "workspace:*",
    "@amamo/verso-linux-arm64": "workspace:*",
    "@amamo/verso-linux-x64": "workspace:*",
    "@amamo/verso-win32-x64": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2"
  }
}
