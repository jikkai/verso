{
  "name": "@univerkit/verso-repo",
  "private": true,
  "description": "A focused release CLI for JavaScript workspace packages.",
  "license": "MIT",
  "author": "DreamNum Co., Ltd. <developer@univer.ai>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dream-num/verso.git"
  },
  "bugs": {
    "url": "https://github.com/dream-num/verso/issues"
  },
  "homepage": "https://github.com/dream-num/verso#readme",
  "engines": {
    "node": ">=22.18.0"
  },
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "prepare": "simple-git-hooks",
    "precommit": "pnpm run format:check && pnpm run lint",
    "build": "pnpm --filter @univerkit/verso build",
    "check": "pnpm run format:check && pnpm run lint && pnpm run check:scripts && pnpm run check:docs && pnpm run check:rust && pnpm run check:npm",
    "check:docs": "pnpm --filter @univerkit/verso-docs types:check && pnpm --filter @univerkit/verso-docs build",
    "check:npm": "pnpm --filter @univerkit/verso typecheck && pnpm --filter @univerkit/verso test",
    "check:rust": "cargo fmt --check && cargo clippy --locked --all-targets --all-features -- -D warnings && cargo test --locked --all",
    "check:scripts": "tsc -p tsconfig.json --noEmit",
    "format": "oxfmt --write scripts packages/verso/src packages/verso/scripts && cargo fmt",
    "format:check": "oxfmt --check scripts packages/verso/src packages/verso/scripts && cargo fmt --check",
    "lint": "oxlint --ignore-path .oxlintignore .",
    "docs:dev": "pnpm --filter @univerkit/verso-docs dev",
    "docs:build": "pnpm --filter @univerkit/verso-docs build",
    "release": "cargo run -p verso"
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm run precommit"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "oxfmt": "^0.61.0",
    "oxlint": "^1.76.0",
    "simple-git-hooks": "^2.13.1",
    "typescript": "^7.0.2"
  }
}
